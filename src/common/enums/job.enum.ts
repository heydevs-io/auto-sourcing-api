export enum WorkingMode {
  NEGOTIATION = 'NEGOTIATION',
  HYBRID = 'HYBRID',
  ON_SITE = 'ON_SITE',
  REMOTE = 'REMOTE',
}

export enum SalaryType {
  MONTHLY = 'MONTHLY',
  YEARLY = 'YEARLY',
}

export enum EmploymentType {
  FULL_TIME = 'FULL_TIME',
  PART_TIME = 'PART_TIME',
  CONTRACT = 'CONTRACT',
  TEMPORARY = 'TEMPORARY',
}
